<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- logo -->
    <link rel="icon" type="image/png" href="/system-assets/logo/h.png" />
    <title>{{ $title ?? 'Introduction' }} | {{ env('APP_NAME', 'Honovel Deno') }} Docs</title>

    <!-- Tailwind Play CDN -->
    <script src="/main/js/jquery.js"></script>
    <link rel="stylesheet" href="/main/style.css">
    @vite(['resources/css/app.css'])
</head>

<body class="bg-gray-50 text-gray-800">
    <!-- Header -->
    <header class="fixed top-0 left-0 right-0 h-14 bg-white border-b z-30 flex items-center justify-between px-4 md:px-8">
        <div class="flex items-center gap-3">
            <!-- Mobile menu toggle -->
            <button id="asideToggle" aria-expanded="false" class="md:hidden p-2 rounded-md hover:bg-gray-100">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            </button>

            <!-- Logo + Title -->
            <div class="flex items-center text-lg font-semibold text-indigo-700">
            <img src="/system-assets/logo/h.png" alt="Honovel" class="h-8 w-8">
            <span class="ml-2">Honovel Deno</span>
            </div>
        </div>

        <!-- Navigation -->
        <nav class="hidden md:flex items-center gap-6 text-sm font-medium text-gray-600">
            @if (isset($myTabs))
                @each ($tab in $myTabs)
                    <a href="{{ $tab.href == '/docs' ? '/docs/' + $contentId : $tab.href }}" class="hover:text-indigo-600 transition {{ $tab.current ? 'active-nav' : '' }}">{{ $tab.name }}</a>
                @end
            @endif
        </nav>

        <!-- Right-side text or version -->
        <div class="text-xs text-gray-500 hidden md:block">v1.0 Documentation</div>
    </header>


    {{{ $slots.content ? await $slots.content() : '' }}}
</body>
</html>
